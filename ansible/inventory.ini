[aiops_targets]
aiops-target ansible_host=192.168.198.130 ansible_user=root

# IP 别名：让 alert.host_ip（IP 形式）直接当成 hosts 也能匹配
# 不加这一行，playbook 里 hosts: "{{ target_host }}" 用 IP 进来会被 ansible 当未知主机
192.168.198.130 ansible_user=root

[all:vars]
ansible_python_interpreter=/usr/bin/python3
